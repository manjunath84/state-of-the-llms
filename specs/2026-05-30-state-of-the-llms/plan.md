# State of the LLMs — Plan (pointer)

The transient, bite-sized TDD task list lives at:

`../../docs/superpowers/plans/2026-05-30-state-of-the-llms.md`

Per the SDD constitution, that plan is **disposable** — the spec (`requirements.md`
in this folder + the `specs/` constitution) is the source of truth. The plan is
kept only as an execution record; when scope changes, the spec changes first and
the plan follows.

Execution method: `superpowers:subagent-driven-development` — a fresh implementer
subagent per task, TDD, with spec-compliance + code-quality review between tasks,
plus a Codex review pass and a final code review.
